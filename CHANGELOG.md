# سجلّ التغييرات

## Unreleased

### فحص أمني وhardening جراحي — 2026-08-22

فحصٌ عميق للمكتبة بأكملها (regex، يونيكود، CLI، مسارات الملفات، معالجة الأخطاء)
بلا أي ميزة جديدة. **١٠٣٤/١٠٤٠** مخرجاً مرجعياً (يشمل كامل corpus الـ Bidi
العدائي بـ١٠٠٠ حالة) متطابق بايت-ببايت قبل وبعد؛ التغيّرات الست كلها من
الإصلاحات الأربعة المقصودة أدناه.

#### الأداء — إسقاط تربيعية مُقاسة كانت مفعّلة افتراضياً

| المسار | قبل | بعد | التحسين |
|---|---:|---:|---:|
| `collapse_midword_spaces` عند n=20k | 19,878ms | 13ms | ~1500× |
| `reverse_visual_line` (جزر LTR) عند n=20k | 4,391ms | 60ms | ~73× |

* انهيار ta-marbuta في hygiene كان يعيد المسح داخل المدى العربي بحثاً عن «ة»
  عند كل موضع (`O(n²)`). أُضيفت مرساة `(?<![\u0621-\u064A])` المطابقة لأخيه
  في القاعدة 1–3، والمخرجات متطابقة.
* نمط جزيرة LTR كان يسمح بتكدّس غير محدود لعلامات الحواف `%#$€£+`؛ فشلُّ
  العثور على ذرّةٍ بعدها كان يجعل `re.sub` يتراجع علامةً بعلامة عند كل موضع.
  حُدّد التكرار بـ`{0,8}` — لا نصٌّ واقعي يكدّس أكثر.
* رُفعت ترجمة أنماط pdf_confusions من داخل الدالة إلى مستوى الوحدة (مرة واحدة
  عند الاستيراد)، وكذلك أنماط hygiene الثلاثة.

#### تلف صامت للنص العربي — مُصلَح

* **lamalef `_WORD`**: النطاق `[0621–064A]` كان يقطّع الكلمة عند الألف
  الوصلية (ٱ U+0671) والألف الخنجرية (ٰ U+0670) وU+0653–U+0655، فيفشل إصلاح
  المعجم بصمت مع رسمٍ قرآنيّ مثل «ٱلمجالت» رغم شهادة المستند نفسه
  (`fixed_by_lexicon 0→1`). صار `[0621–06D3]`.
* **حركات التركيب U+0653–U+0655**: فئات الحركات في hygiene وpdf_confusions
  لم تكن تعدها علامات، فتبقى مسافةٌ قبل همزة NFD المركّبة («بر ٔزق»). صارت
  الفئات `[\u064B-\u0655\u0670]` موافِقةً لـ`scientific._TASHKEEL`.
* **cmap**: تعارض جليفٍ واحد مع رابط لا-ألف كان يرمي TypeError من
  `is_arabic('لا')` ويبتلعه `except` الصامت فيضيع بقية جدول الخط كله؛ وفُحص
  الطول الآن. كما ترفض `decode_glyph_name` البدائل المعزولة (uniD800) وما بعد
  سقف يونيكود التي كانت تكسر ترميز UTF-8 لاحقاً.
* **U+FEFF** استُبعد من مجموعة أشكال العرض السريعة موافَقةً للحذف الصريح
  الموجود في الجداول المرجعية نفسها؛ لم تعد `diagnose` تبلغ عيب
  presentation_forms زائفاً عن نص BOM مكرر.

#### حواجز CLI ومعالجة أخطاء

* **stdio UTF-8**: أنابيب Windows كانت تفكّ stdin بترميز cp1256/cp1252 —
  انهيارٌ أو تلفٌ صامت ثم «إصلاح» لنصٍّ تالف اصطناعياً. `main()` تثبّت
  UTF-8 على المداخل والمخارج (stdout/stderr بـerrors=replace).
* **`extract -o`**: رفضٌ صريح عندما يطابق المخرج المصدر — كان يمحو الـPDF
  الأصلي نهائياً بعد اكتمال الاستخراج.
* **eval --compare**: فشل جميع المستخرجات كان يرمي IndexError خاماً؛ تُرفع
  RuntimeError ودّية، وأخطاء المستخرجات الفردية تطبع على stderr لا stdout
  حفاظاً على قابلية التحليل الآلي للتقارير.
* **diagnose -n**: تُرفض الأعداد السالبة (كانت تُقبَل وتنتج صفحةً واحدة
  بصمت)، وملفّ الحقيقة في `evaluate_pdf` يُقرأ بـutf-8-sig فلا يحسب BOM
  خطأً في CER.
* **pipeline**: سبب فشل بناء خرائط CMap يُسجَّل في
  `metadata["cmap_build_failed"]` بدل ابتلاعه.

#### قرارٌ موثَّق بلا تغيير سلوكي

Mn محصورٌ بين قاعدتي حرفين في سطرٍ بصري متناقض بطبيعته: الاتفاقية
العنقودية تجعله للقاعدة السابقة، والانعكاس الخام يجعله للتالية. الالتزام
القائم بالاتفاقية العنقودية بقي قائماً — قلبُه كان سيُفسد مصادرَ حقيقية
تتبعها — والغموض موثَّق في docstring ‏`grapheme_clusters` مع اختبارِ
تثبيتٍ صريح.

#### الاختبارات

٣٥٧ ناجحة (٣٣٧ قائمة + ٢٠ انحداراً جديداً في `tests/test_audit_hardening.py`
تغطي كل ما سبق بالقياس)، ولا تبعيات جديدة.

### تعديلات جراحية وتحسينات استخراج PDF العربي — 2026-08-18

أُجريت مجموعة من التعديلات الذرية العامة على مسار استخراج وإصلاح النص العربي، مع الإبقاء على رقم الإصدار المعلن **1.0.1**. شملت التغييرات حماية النصوص اللاتينية والمعرّفات من الإصلاح الزائد، تضييق إصلاحات المسافات والالتباسات الناتجة عن PDF، تحسين إعادة بناء glyphs العربية في حالات الترتيب الهندسي لام/ألف، وتمرير إعدادات layout من pipeline إلى المستخرج بدلاً من إنشاء إعدادات صامتة داخله.

كما خُفّضت إعادة بناء layout وإعادة تشغيل مراحل الإصلاح الكاملة بعد إصلاح blocks، وأضيف cache لنصوص الأسطر، وحُسّن حساب فواصل الأعمدة بإحصاءات تراكمية، ووُحّد تحديث التشخيص وaudit بعد التعديلات المتأخرة. هذه التغييرات حسّنت السرعة من دون تراجع مقاس في الاختبارات المنشورة.

اختير ملف RTF عربي عشوائياً ونُزّل من **المكتبة الشاملة**، ثم حُوّل محلياً إلى PDF من ثماني صفحات لاستخدامه كمرجع فحص مستقل. في آخر فحص مقابل النص المرجعي كانت النتائج: **CER = 2.1879%** و**WER = 7.8017%**، مع استخراج 12,619 محرفاً، ومن دون replacement characters أو presentation forms أو private-use characters أو soft hyphens أو zero-width characters.

تفاصيل المستند ونتيجة القياس محفوظة في [`benchmarks/rtf_eval/`](benchmarks/rtf_eval/).

### Evidence pipeline architecture — 2026-08-12

أُعيد ربط `DocumentContext` بمحرك الاسترجاع بدلاً من اعتباره API مستقلاً:
`detector → CandidateGenerator → evidence sources → EvidenceFusion → decision → repair → audit`.
يبقى `DocumentContext.repair()` متاحاً للتوافق، لكنه يستخدم داخلياً نفس مولّد
المرشحين وقرار الدمج؛ ولا يملك أي detector منفرد صلاحية فرض الإصلاح.

أضيفت طبقة stdlib-only في `arafix.evidence`:

| المكوّن | الدور |
|---|---|
| `CandidateGenerator` | جمع المرشحين من document vocabulary ومسافة تحرير واحدة، ومصادر confusion وPDF وglyph؛ لا يتخذ قراراً |
| `Confusion` / `CharacterConfusionModel` | تمثيل observed→candidate مع `source` و`cost`؛ لا يعمل تلقائياً إلا عند الحقن الصريح |
| `GlyphEvidence` | حمل font وglyph_id وbbox ودرجة الدليل إلى candidate layer؛ لا يفرض تصحيحاً |
| `NegativeEvidenceModel` | حماية URL/email والـidentifiers والنص المقتبس وجزر Latin/code الفعلية |
| `EvidenceFusion` | دمج الإشارات وإخراج `SAFE` أو `UNCERTAIN` أو `UNSAFE` عبر عقد `RepairDecision` المركزي |

أضيفت إلى `DocumentContext` إحصاءات word trigrams وcharacter 4-grams وparagraph-local
vocabulary، مع إبقاء word frequency وbigrams وcharacter trigrams. أضيفت حقول
`fusion_decisions` ومرشحات الأدلة إلى `ContextRepair` وaudit. الإضافات لا تضيف
تبعية تشغيلية، ولا تُفعّل CharacterConfusion أو glyph-derived candidates افتراضياً.

| بوابة القياس | النتيجة |
|---|---:|
| constitution mutation recovery | **61/61 exact**، CER **0%**، WER **0%** |
| narrative mutation recovery | **57/57 exact**، CER **0%**، WER **0%** |
| baseline exact recovery | **0/118** |
| safe corpus | **0/18 false repairs**؛ FPR **0%** |
| clean real-document no-op | SHA-256 وسلوك النص محفوظان |
| full regression suite | جميع الاختبارات ناجحة بعد إضافة عقود evidence |

تم إلغاء أي تصحيح تلقائي لمحرف عادي مستند إلى glyph shape لأن لا توجد fixture
PDF معلّمة تثبت تحسناً في CER/WER. كما بقيت CharacterConfusionModel وPDF confusion
generator وGlyphEvidence مصادر مرشحين قابلة للحقن، لا قواعد إصلاح افتراضية.

أضيف حارس يمنع Context من مطابقة prefix داخل كلمة تحتوي حركة داخلية، كما رُفعت
حماية النص المقتبس إلى `UNSAFE` فعلياً، وبقيت عناوين عربية المنتهية بنقطتين خارج
حماية code-island. بعد ذلك بقيت النتائج **61/61** و**57/57**، وFPR **0%**،
وبقي SHA-256 للـPDF الحقيقي مطابقاً في المسارين off/on.

---

### Context Scoring and Glyph Evidence evaluation — 2026-08-12

أضيفت طبقة `DocumentContext` اختيارية تعتمد على المكتبة القياسية فقط. تبني
معجم الوثيقة، تكرار عبارات الكلمتين، وcharacter trigrams، ثم تولّد مرشحين
بمسافة تحرير واحدة من الكلمات الموجودة في الوثيقة. لا يُقبل المرشح إلا عند
اجتماع تكرار الكلمة، ودعم العبارة، والدليل الحرفي، وهامش واضح عن البديل؛
وإلا تمتنع الطبقة. لا تُستخدم LLM ولا `transformers` ولا أي تبعية تشغيلية
جديدة.

أضيف `PipelineConfig.enable_context_scoring` وهو مطفأ افتراضياً، و`context_model`
للاستخدام البرمجي المباشر. في `extract_pdf()`، عند التفعيل بلا نموذج، يُبنى
نموذج واحد من صفحات الوثيقة ويُطبّق على الصفحات مرة واحدة. تسجل المرحلة
`Stage.CONTEXT` وأحداث audit `DOCUMENT_CONTEXT_SCORING` مع المرشحين والقرار
والرقعة القابلة للعكس.

| القياس | النتيجة |
|---|---:|
| constitution text mutations | **61/61 exact**، CER **0%**، WER **0%** |
| narrative text mutations | **57/57 exact**، CER **0%**، WER **0%** |
| baseline exact recovery على نفس التحويرات | **0/118** |
| baseline mean CER | 0.0197% constitution، 0.1668% narrative |
| baseline mean WER | 0.1203% constitution، 1.3889% narrative |
| safe stress cases | **0/18 false repairs**؛ FPR **0%** |
| real PDF no-op | SHA-256 مطابق في الملفين؛ **0 pages touched** |
| optional context overhead | median 0.1499s off مقابل 0.2004s on في constitution؛ feature opt-in ولا يثقل default |

هذه النتائج تخص تحويرات نصية seeded مبنية من نصوص مرجعية حقيقية، ولا تدّعي
أنها corpus PDF معطوبة. لذلك بقي `DocumentContext` opt-in، ولم يُفعّل في
المسار الافتراضي.

أما Glyph Evidence، فقد ثبت توفر `glyph_id` واسم الخط والحجم وbbox وخريطة
CMap في ملفات PDF الحقيقية، مع 5,895 ملاحظة glyph في الدستور و6,628 في الملف
السردي، و0 هويات `(font, glyph_id)` ملتبسة بعد تطبيع الأشكال الرسومية. كما
كُشف وأُصلح تطبيع subset-prefix لأسماء الخطوط في `_canonical_font_name`. لكن
لم توجد fixture PDF معلّمة تربط محرفاً Unicode خاطئاً بشكل glyph صحيح، ولذلك
لم يُضمّن تصحيح آلي لمحارف عادية ولم يُدّعَ أي تحسن CER/WER لهذه المهمة؛ لا
يجوز تحويل توفر الدليل إلى إصلاح تخميني.

---

### Consistency review and contract hardening — 2026-08-12

أُجريت مراجعة اتساق شاملة على وحدات الحزمة والاستدعاءات والـexports. شملت
الفحوصات `ruff` للاسماء والاستيرادات، و`mypy` على 26 وحدة، واستيراد جميع
الوحدات وفحص `__all__` وقت التشغيل، وsmoke tests لأوامر CLI، وvulture
للكود غير المستخدم عالي الثقة، إضافة إلى اختبار السلوك الكامل.

كُشف وأُصلح خلل حقيقي في عقد التدقيق: كان `harvest_document_lexicon` يعدّل
`PageResult.text` بعد إنشاء audit، فيبقى `repaired_sha256` والـpatch محسوبين
على النص السابق. كان ذلك يؤدي إلى رفض العكس برسالة `patch target SHA-256
 does not match repaired_sha256` على صفحة أصلحها معجم الوثيقة. أصبح الإصلاح
اللاحق يسجل حدث `DOCUMENT_LEXICON_LAM_ALEF` ويعيد بناء الرقعة الكاملة من
`PageResult.original` إلى النص النهائي، مع الحفاظ على أوضاع `summary` و`full`.
أضيف regression test بصفحتين ومستخرج اختباري يثبت التطبيق والعكس والهاش.

صُححت أيضاً عقود النوع التي كانت رخوة أو متناقضة مع الاستدعاء الفعلي:
أصبح `_recover_broken_cmap_page` يعيد `RawPage` ويستقبل `Mapping[str,
GlyphMap]`، وأصبح bbox في مستخرج PyMuPDF tuple رباعياً صريحاً أو `None`،
وعولجت تقارير CLI بنوع `list[dict[str, Any]]`. أزيلت `type: ignore` غير
المستخدمة، وحُذف وسيط `col_width` الميت من كشف الجداول وحساب `col_w` الزائد،
وأزيل ثابت `LINE_TOLERANCE` غير المشار إليه.

أضيف فحص `mypy src` إلى CI، وثُبّت في dev extra عند `mypy>=1.8,<2` حتى يبقى
متوافقاً مع Python 3.9 المعلن. أما الدوال `normalize_result` و`fix_order_result`
فقد راجعت ولم تُحذف: هما أغلفة convenience غير مستعملة داخلياً، لكن حذفهما
قد يكسر مستعملاً يستورد الوحدة مباشرة؛ لم تُعتبر مفقودة أو خاطئة، ولم تُضف
إلى سطح API العام بلا حاجة.

| بوابة التحقق | النتيجة |
|---|---:|
| ماسح الاستيرادات و`__all__` وقت التشغيل | **CONSISTENCY_OK**؛ 26 وحدة، 109 exports في package root |
| `ruff check src tests examples` | ناجح |
| `mypy src` | **Success: no issues found** |
| مجموعة الاختبارات الكاملة | **317/317 ناجحة** |
| اختبارات العقود والـregression | ناجحة |
| smoke tests لـ`diagnose`, `extract`, `eval`, `text`, `blocks`, `fonts` | **7/7 ناجحة** عبر `python -m arafix.cli` |
| إزالة `col_width` وحسابات التخطيط الميتة | مخرج PDF وSHA-256 مطابقان |
| benchmark layout alternating | فرق صغير متذبذب، نحو **0–2%**؛ لم يُعتبر تحسناً ملحوظاً |
| تبعيات runtime | لم تتغير؛ لا تبعية جديدة للحزمة |

لم تُرفع أرقام أداء جديدة إلى README لأن القياس المتناوب لم يثبت نقلة
ملحوظة؛ التغيير مقبول لجودة العقد وتقليل الالتباس، لا كادعاء throughput.

---

### Recovery Audit Engine and safety evaluation — 2026-08-12

أضيفت طبقة تدقيق اختيارية لا تغيّر مخرج `repair_text()` الافتراضي. يعرّف العقد
`arafix.recovery-audit.v1` الأنواع `AuditMode` (`off` و`summary` و`full`)،
وقرارات `SAFE` و`UNCERTAIN` و`UNSAFE`، و`EvidenceItem` و`AuditEvent` و`Patch`
و`RepairAudit`. الوضع `off` لا ينشئ كائن audit ولا رقعة؛ `summary` يسجل أحداثاً
على مستوى المراحل دون حفظ المقاطع؛ و`full` يسجل المقاطع المتغيرة ويصدر رقعة
محمية بـSHA-256.

تم ربط السجل بالقواعد ذات الأدلة الصريحة في `repair_text()`: hygiene،
mojibake الحتمي، presentation forms، إعادة الترتيب، الرباطات المؤجلة،
لام-ألف، PDF homoglyphs، الفراغات السياقية، وقائمة التباسات PDF المغلقة.
كما تُسجل حالات الامتناع صراحةً: لام-ألف الملتبس كـ`UNCERTAIN`، والأشكال
الرسومية منخفضة الكثافة كـ`UNCERTAIN`، وCMap المكسورة أو غياب طبقة النص
كـ`UNSAFE`. الامتناع لا يغيّر النص تلقائياً.

الرقعة تتحقق من هاش المصدر قبل التطبيق ومن هاش الناتج قبل العكس. لذلك يمر
المسار `original → apply(patch) → repaired → revert(patch) → original`، وترفض
الرقعة نصاً مختلفاً بدلاً من تطبيقها على محتوى غير مطابق. لم تُدعَ قابلية
العكس للمراحل التي لا تملك mapping حرفياً مستقلاً؛ `full` يضمن رقعة النص
الكاملة، بينما الأحداث المركبة تُفهم على مستوى المرحلة.

أضيفت أدوات تقييم مستقلة عن runtime: `scripts/audit_corpus.py` لقياس no-op
preservation وFalse Repair Rate وexact recovery والامتناع والعكس، ومحرك تحوير
نصي seeded في `scripts/mutation_engine.py` مع benchmark L0. التحوير النصي لا
يدّعي محاكاة أخطاء CMap أو watermark أو ترتيب الأعمدة أو الجداول متعددة الصفحات؛
هذه مؤجلة إلى fixtures PDF ذات provenance واضح.

| بوابة التحقق | النتيجة |
|---|---:|
| `ruff check src tests` باستخدام Ruff 0.4.10 | ناجح |
| مجموعة الاختبارات الكاملة | **315/315 ناجحة** |
| Audit stress scope (`repair_text` فقط، 33 حالة) | **FPR = 0%**، **RAR = 100%**، **16/16** استرجاعاً، **0** فشل عكس |
| Mutation L0 المدعوم | **10/10** exact recovery، **12/12** revert |
| Mutation L0 المشروط | **2/2** حالات امتناع مسجلة صراحةً |
| تطابق `audit=off` مع `summary/full` | مطابق في جميع العينات |
| `summary` overhead على النصوص المرجعية | نحو **-1.5% إلى +2.5%** في القياس الحالي، مع ضوضاء القياس |
| `full` overhead | مرتفع على النصوص القصيرة؛ بقي اختيارياً ولم يُجعل افتراضياً |
| تبعيات runtime | لم تتغير؛ التنفيذ يعتمد على المكتبة القياسية فقط |

النتيجة السلبية المهمة موثقة ولا تُخفى: `full` يضيف كلفة ملحوظة عند حساب
الفروق والرقع، خصوصاً على نصوص قصيرة، ولذلك لم يُفرض على المسار الافتراضي.
كما لم نرفع أرقاماً مثل `confidence=0.997` بوصفها احتمالات معايرة؛ القيم
الحالية درجات قوة دليل فقط. ولم نضف `arafix explain` بعد، لأن بناء واجهة عرض
قبل استقرار عقد audit سيضاعف السطح البرمجي دون فائدة مثبتة.

---

### Native Spatial RAG-Chunking — 2026-08-12

أضيف مخرج اختياري جاهز لأنظمة RAG عبر `extract_pdf_rag()`، كما أضيفت الواجهتان `DocumentResult.to_rag_chunks()` و`DocumentResult.to_rag_json()`. كل `RAGChunk` يحمل `id` و`text` بعد الإصلاح، ورقم الصفحة، و`bbox` بالإحداثيات، و`role`، و`parent_context`، و`source`، و`metadata`. الجداول تُخرج خلايا مستقلة مع أرقام الصف/العمود، والعناوين تفتح سياقاً أبوياً، والأسطر القريبة تُجمع حتى الفاصل الهندسي أو `max_chars`.

التجزئة **structure-aware deterministic** وليست ادعاءً بوجود نموذج embeddings أو فهم دلالي خارجي. لا تُضاف Torch أو Transformers أو قاعدة متجهات. حفظ bbox الرسم الأصلي مطفأ في `extract_pdf()` ومفعّل فقط داخل `extract_pdf_rag()`، لذلك لا تتغير واجهة الاستخراج التقليدية ولا تُدفع كلفة bbox إلى المستخدم الذي لا يطلب RAG. عقد JSON هو `arafix.spatial-rag.v1`، و`bbox` بالترتيب `[x0, y0, x1, y1]` وبوحدة نقاط PDF.

| بوابة التحقق | النتيجة |
|---|---:|
| اختبارات RAG الجديدة | **4/4 ناجحة** |
| مجموعة الاختبارات الكاملة | **305/305 ناجحة** |
| Ruff 0.4.10 على `src tests` | ناجح |
| Branch coverage | **89%**؛ الانخفاض ناتج عن الوحدة الاختيارية الجديدة، لا عن فقد تغطية المسار القديم |
| PDF حقيقي، 4 صفحات | chunks مكانية صالحة، IDs فريدة، صفحات وbboxes مرتبة |
| PDF إجهاد، 2000 صفحة، `extract_pdf()` العادي | SHA-256 مطابق حرفياً: `896b3c581045401360c154deff3d6bd998bfb9d06fe6fbd5a636daf21b49d520` |
| زمن 2000 صفحة، median | `2.2777 s` قبل / `2.3115 s` بعد؛ فرق ~1.5% ضمن تذبذب القياس، مع إخراج مطابق |
| تبعيات runtime | لم تتغير؛ `dependencies = []` |

القيود مقصودة: لا يعالج RAG الصفحات الممسوحة ضوئياً، ولا يستنتج عناوين دلالية من نموذج لغوي، ولا يضمن أن كل PDF يملك bbox أصلياً؛ عند غياب geometry يرجع bbox صفحة كاملة كـfallback. هذه نسخة محافظة تصلح كطبقة citation-ready فوق النص المسترد، بينما اختيار embeddings والـvector store يبقى للنظام المستهلك.

---

### إصلاح بوابة Ruff في main — 2026-08-12

أظهرت سجلات CI المرفقة أن جميع بيئات المصفوفة كانت تتوقف قبل `pytest` في خطوة `ruff check src tests` برسالة `Found 25 errors`. كانت الأخطاء `I001` و`E501` و`UP035` و`SIM102` و`F401` في ملفات موجودة مسبقاً، ولم يظهر `Traceback` أو `ModuleNotFoundError` أو فشل استخراج؛ لذلك لم يكن السبب تبعية ناقصة أو خللاً في منطق arafix.

أُصلحت المخالفة بصورة محافظة عبر ترتيب imports ولفّ الأسطر الطويلة وإزالة import غير مستخدم فقط، مع دمج شرطيْن مكافئين منطقياً في `order.py`. وثُبّت Ruff في dev extra عند `>=0.4,<0.5` حتى لا تكشف إصدارات أحدث مخالفات baseline غير مرتبطة بالسلوك. لم تُضف أي تبعية إلى `dependencies = []`، ولم تتغير extras التشغيلية (`pdf` و`cmap` و`markitdown` و`all`).

| بوابة التحقق | النتيجة |
|---|---:|
| `ruff check src tests` باستخدام Ruff 0.4.10 | ناجح |
| الاختبارات الكاملة | **301/301 ناجحة** |
| Branch coverage | **90%** |
| `python -m build` و`twine check --strict` | ناجحان |
| متطلبات wheel الأساسية | لا شيء؛ جميع `Requires-Dist` مرتبطة بـextras اختيارية فقط |

هذا الإصلاح لا يغيّر ناتج الاستخراج أو قواعد إصلاح العربية؛ وظيفته إعادة CI إلى فحص قابل للتنفيذ ثم السماح للاختبارات الفعلية بالعمل.

---

### جراحة الأداء وWER — 2026-08-12

تم دمج الإصلاحات المقبولة إلى `main` عبر [PR #7](https://github.com/bio-colab/arafix/pull/7)، والالتزام المدمج هو `f85103b9d7824af1d6078dc9005cd33ee5ddcf8e`.

#### تحسين ضم الجليفات

أُعيد تنظيم `join_glyphs_preserving_ltr` ليبني نص كل token مرة واحدة، ويحتفظ بحدود الجليفات فقط عندما يحتاج المسار إلى حساب الفجوة الهندسية. لم يتغير ترتيب LTR، ولا التعامل مع `seq`، ولا أولوية الفراغات الصريحة.

| القياس على ملف إجهاد 2000 صفحة | `origin/main` | المرشح | الأثر |
|---|---:|---:|---:|
| عدد الأسطر | 4000 | 4000 | ثابت |
| عدد المحارف | 216893 | 216893 | ثابت |
| SHA-256 للنص | `42cccbfecd175b95bc02c7c446fa204b74233c42d85cbdb1f9961a8476dc1dc1` | نفس البصمة | تطابق حرفي |
| median ضم الخطوط، القياس الحاسم | 0.064642 s | **0.064154 s** | تحسن ~0.8% |
| median استخراج الملف كاملاً | 2.55980 s | **2.47393 s** | تحسن ~3.35% |

أظهرت إعادة تشغيل `cProfile` أن المسارات الساخنة ما زالت `extract_pdf` و`_extract_one_page` و`pages` و`join_glyphs_preserving_ltr` و`repair_text`. هذا تحسين موضعي مقاس، وليس ادعاءً بأن كل عنق الزجاجة اختفى.

#### تجربة cache التخطيط — مسحوبة

تكرر التوقيع الهندسي للصفحات في ملف الإجهاد، لكن بناء وفرز توقيع `(x,y)` قبل lookup أضاف كلفة أكبر من التوفير. ارتفع زمن `extract_pdf` تحت profiler من **6.281 s** بعد batch join إلى **6.799 s** مع cache مؤقت. لذلك أُزيل cache بالكامل ولم يُحتفظ بأي تخزين قد يعيد كائنات `PageLayout` بين صفحات مختلفة.

#### إصلاح الترقيم والفراغات السياقي

أضيفت `normalize_arabic_punctuation_spacing` إلى مرحلة `REPAIR_SPACING`. تعمل القاعدة فقط عندما يثبت حرف عربي السياق؛ فتزيل الفراغ قبل الترقيم، وتضبط فراغاً واحداً بعده قبل حرف عربي، وتضيف فراغاً قبل القوس المفتوح بعد حرف عربي عند غيابه. لا تلمس فواصل Latin مثل `GDP,2024`، ولا الكسور العشرية مثل `3.14`، ولا تعبر فواصل الأسطر.

| ملف الدستور | CER | WER |
|---|---:|---:|
| خط الأساس قبل القاعدة | 3.486% | 20.939% |
| بعد الترقيم السياقي | **3.092%** | **17.208%** |
| تعطيل spacing بالكامل | 3.545% | 21.059% |
| تعطيل Hygiene | 3.841% | 18.893% |
| تعطيل reorder | 78.787% | 99.759% |

التحسن يساوي 0.394 نقطة مئوية في CER و3.730 نقطة مئوية في WER، أي خفض نسبي يقارب 17.8% من WER الأساسي. أضيف اختبار انحدار يثبت الإصلاح العربي وعدم تغيير النص اللاتيني والعشري وفواصل الأسطر.

#### تجربة إرفاق التشكيل — مسحوبة

جُرّبت قاعدة تقصر إرفاق العلامة على القواعد الموجودة ضمن tolerance السطر نفسه. اجتازت اختبارات التشكيل وPDF الحقيقي، لكنها لم تغيّر CER أو WER في ملف الدستور: بقيت النتيجة 3.092% / 17.208%. لذلك سُحبت التجربة ولم يُعدّل `pymupdf_extractor.py`، تجنباً لإضافة قاعدة قد تفقد علامات صحيحة دون مكسب مثبت.

#### التحقق

اجتازت النسخة **301/301 اختباراً**، وبلغت Branch coverage **90%**، واجتاز التغيير `git diff --check`. لم تُضمّن ملفات `pstats` أو ملفات القياس المؤقتة أو `benchmark_results` في الالتزام.

---

### تحصين Bidi وفلترة الضوضاء الهندسية وتشريح الأداء — 2026-08-12

تم دمج هذه التحسينات إلى `main` عبر [PR #5](https://github.com/bio-colab/arafix/pull/5)، والالتزام المدمج هو `19316e56b28eb5a105709ac4aa1c20e579f057fd`. أُجريت القياسات على نسخة نظيفة من `main`، ثم اجتازت مجموعة الاختبارات الكاملة **300/300 اختباراً** مع **Branch coverage = 90%**.

#### تحصين الجزر اللاتينية في Bidi

أضيفت حماية `solid-block` إلى `arafix.order` حتى تُعامل البنى الرقمية واللاتينية ذات المعنى كوحدة واحدة بعد إعادة ترتيب السطر. تشمل الحماية التواريخ ذات الشرطات، الإصدارات، أرقام الهواتف، البريد الإلكتروني، المصطلحات الهجينة، ونطاقات الصفحات المجاورة لـ`ص.` أو `p.`. كما عولجت علامات الجملة الطرفية حتى لا تتحول الساعة `10.` إلى `01.`، وبقي التمييز بين تاريخ `13-7` ونطاق صفحات مثل `125-140` قائماً على السياق والقياس، لا على عكس أعمى.

| فئة corpus الخصومي | الحالات | خط الأساس قبل الإصلاح | بعد الإصلاح |
|---|---:|---:|---:|
| التواريخ (`13-7-2024`) | 250 | 0/250 (0%) | 250/250 (100%) |
| الإصدارات (`v1.0.1-beta`) | 250 | 31/250 (12.4%) | 250/250 (100%) |
| المصطلحات الهجينة، ومنها `GDP 2024` و`M/V Ever Given` والبريد الإلكتروني | 250 | 0/250 (0%) | 250/250 (100%) |
| أرقام الهواتف | 250 | 63/250 (25.2%) | 250/250 (100%) |
| **الإجمالي** | **1000** | **94/1000 (9.4%)** | **1000/1000 (100%)** |

القائمة حتمية ومضمّنة في `benchmarks/adversarial_bidi_corpus.json`، واختبارها في `tests/test_hardening.py`. تم تشغيل اختبارات الانحدار الخاصة بالسنوات، الجزر اللاتينية، التواريخ، نطاقات الصفحات وstress corpus، ولم تُقبل القاعدة إلا بعد اجتيازها.

#### فلترة الضوضاء الهندسية قبل خط الإصلاح

أضيفت `src/arafix/noise.py` وربطت بمسار PyMuPDF عبر `PipelineConfig.geometric_noise`. يعتمد الفلتر على metadata فعلية من `get_texttrace()`، ولا يحذف النص اعتماداً على محتواه العربي. الدليل الافتراضي القوي هو اجتماع اللون الرمادي الفاتح مع الدوران وحجم span؛ أما كشف المحارف القصيرة المتكررة في موضع واحد عبر صفحات متعددة فهو خيار صريح (`remove_repeated_short_spans=True`) لأنه يحتاج مروراً ثانياً على traces.

| اختبار PDF الضوضاء، 3 صفحات | النتيجة |
|---|---:|
| spans رمادية مائلة أزيلت | 3، جميعها `light-gray-rotated` |
| بقايا نص watermark في الناتج | لا |
| المحارف صفرية العرض في الناتج | لا |
| الجداول قبل/بعد | 3 / 3 |

الفلتر احتُفظ به لأنه أزال watermark فعلياً من PDF الاختبار، مع توثيق كلفته وحدوده. في قياس wall-clock واحد على ملف 2000 صفحة كان الزمن 3.05 ثانية مع الفلتر مقابل 2.49 ثانية بدونه؛ لذلك لم يُفعّل كشف التكرار متعدد الصفحات افتراضياً، ولا يُدّعى أن الفلتر بلا كلفة.

#### تشريح عنق الزجاجة بـ`cProfile`

شُغّل `cProfile` على ملف إجهاد من 2000 صفحة. في تشغيل الفلتر المفعّل بلغ الزمن التراكمي لـ`extract_pdf` نحو 7.132 ثانية تحت profiler. المسارات الأعلى كانت إعادة بناء النص وضم الجليفات والتخطيط، لا CMap:

| المسار | الموضع | الزمن التراكمي تحت profiler |
|---|---|---:|
| `_extract_one_page` | `pipeline.py:641` | 3.160 s |
| `pages` | `pymupdf_extractor.py:226` | 3.047 s |
| `join_glyphs_preserving_ltr` / `plain_text` | `layout.py:119,221,265,337` | نحو 1.57–1.63 s |
| `repair_text` | `pipeline.py:181` | 1.494 s |
| `analyze_layout` | `layout.py:649` | 1.083 s |
| `diagnose` | `diagnose.py:504` | 0.854 s |
| `_extract_glyphs` | `pymupdf_extractor.py:103` | 0.510 s |

لم يظهر مسار CMap ضمن المسارات الساخنة؛ لذلك لم يُضف امتداد C/Rust ولم يُستبدل CMap دون دليل. الأولوية المستقبلية، إذا لزم تحسين throughput، هي تقليل إعادة ضم الجليفات وإعادة حساب layout والنص.

#### تشريح WER في ملف الدستور

هذه نتيجة مستقلة لملف `tests/fixtures/real_pdf_narrative/iraq_constitution.pdf` ومرجعه، وليست استبدالاً لنتائج benchmark السابقة في هذا السجل. القياس الحالي أعطى **CER = 3.486%** و**WER = 20.939%**. أظهرت تجربة العزل أن Hygiene وspacing الحاليين يحسّنان النتيجة:

| الإعداد | CER | WER |
|---|---:|---:|
| الإعداد الافتراضي | 3.486% | 20.939% |
| تعطيل `enable_spacing_repair` | 3.545% | 21.059% |
| تعطيل `enable_hygiene` | 4.235% | 22.503% |
| تعطيل `enable_reorder` | 79.141% | 103.851% |

بمحاذاة الكلمات، توزعت وحدات الخطأ المرصودة إلى 57 اختلافاً في الترقيم/الفراغات، و36 حالة تشكيل أو إرفاق تشكيل، و19 حالة ترقيم/مسافة داخلية، و44 حالة اختلاف تسلسل أو تخطيط، و16 حالة دمج/تقسيم حدود كلمات، وحالتين من المحتوى/التخطيط. من الأمثلة `أوالاجتماعي` بدلاً من `أو الاجتماعي`، و`المادة(١٧)` بدلاً من `المادة (١٧)`، و`ق واعد` بدلاً من `قواعد`، إضافة إلى `أولاً` مقابل `اولا` و`لأم` مقابل `لاٍُم`.

بناءً على هذا الدليل، **لم تُضف قاعدة Hygiene جديدة عمياء**؛ لأن تعطيل Hygiene أو spacing أسوأ، ولأن الجزء المتبقي من WER مرتبط بدرجة كبيرة بمحاذاة المصدر والتخطيط والترقيم. هذا القرار مقصود وموثق، وليس تجاهلاً للخطأ.

#### الاختبارات والحدود والملفات

| البند | الحالة |
|---|---|
| الاختبارات المحلية | 300/300 ناجحة |
| Branch coverage | 90% |
| corpus Bidi | 1000/1000 تطابق تام |
| فحص watermark هندسي | 3 spans محذوفة، والجداول محفوظة |
| امتداد C/Rust | لم يُنفذ؛ لم يثبت profiler الحاجة إليه |
| قاعدة Hygiene إضافية للدستور | لم تُنفذ؛ ablation لم يثبت فائدتها |
| ملفات `pstats` ونتائج القياس المؤقتة | خارج الالتزام |
| ملفات `benchmark_results` | لم تُضف إلى الالتزام |

---

### إصلاحات PDF/CMap والفراغات والأداء — 2026-08-12

تم دمج هذه الإصلاحات إلى `main` عبر [PR #3](https://github.com/bio-colab/arafix/pull/3)، بعد قياسها على benchmark النصي، وPDF مولّد، وPDF حقيقي، ثم اجتياز الاختبارات الكاملة.

#### استرداد CMap محافظ من الخط المضمّن

- أضيفت إلى `GlyphMap` خريطة `glyph_id → Unicode` إلى جانب خريطة أسماء الجليفات. هذا يعالج الحالة التي يكون فيها `ToUnicode` تالفاً، بينما يبقى رقم الجليف قابلاً للربط بجدول `cmap` في الخط المضمّن.
- يمرّر `PyMuPDFExtractor` الآن رقم الجليف واسم الخط مع كل جليف هندسي، مع الحفاظ على توافق المستخرجين الخارجيين الذين ترسل جليفات بصيغة `(y, x, text, size)` فقط.
- يطبّق الأنبوب الاسترداد على `PUA` و`U+FFFD` فقط، وعندما تتوافر خريطة موثوقة تطابق الخط ومعرّف الجليف. لا يوجد تخمين لغوي ولا تحويل عام للمحارف السليمة.
- أضيف حارس تعارضات: إذا استُخدم رمز PUA نفسه مع معانٍ مختلفة في خطوط متعددة داخل الصفحة، لا يُطبّق استبدال نصي عام غير آمن؛ تبقى الخريطة الهندسية لكل جليف متاحة للمسار البنيوي.
- أضيفت اختبارات CMap مع خط عربي حقيقي واختبار انحدار للاسترداد الجزئي، وارتفعت تغطية `cmap.py` من 38% إلى 88% وتغطية `pymupdf_extractor.py` من 16% إلى 92%.

#### إصلاح استدلال الفراغات في PDF العربي

- إذا احتوى السطر على `glyph` فراغ صريح، تُعتمد هذه الإشارة ولا تُضاف فراغات هندسية أخرى من فروق الإحداثيات. يمنع ذلك تجزئة كلمات مثل `دراسة` إلى `درا سة` بسبب اختلاف عروض الجليفات في الخطوط المشكّلة.
- عُدّلت `collapse_midword_spaces` لتتعامل مع الحركات والشدة كجزء من الحرف المجاور، فلا تُدمج حدود صحيحة مثل `حين شنّت` و`مَن قرّر`.
- جُمعت قواعد الفراغات المغلقة المحافظة في أنماط Regex مسبقة التجميع، مع الحفاظ على نفس القائمة والحدود والسلوك. تمت مقارنة المخرجات قبل وبعد الدمج على الصفحات الخمس عشرة، وكانت مطابقة حرفياً.
- أضيفت اختبارات للفراغ الصريح، والكلمات العربية المشكّلة، وسلوك مرحلة الفراغات.

#### إزالة كلفة مكررة في التشخيص

- لم يعد `repair_text` يعيد تشغيل كاشف الموجيبيك على النص السليم بعد أن يكون `diagnose` قد فحصه. يُعاد الفحص فقط عندما يثبت التشخيص وجود عيب موجيبيك، مع إعادة التشخيص بعد الاسترداد كما كان مطلوباً.
- اجتاز النص السليم اختباراً يثبت عدم استدعاء الكاشف المكرر، بينما بقيت اختبارات إصلاح الموجيبيك ناجحة.

#### القياس والنتائج

| القياس | قبل | بعد | الأثر |
|--------|----:|----:|-------:|
| `repair_text` على 15 صفحة | 680.730 ms | **590.011 ms** | **13.3% أسرع** |
| `repair_text` على نص موحّد طويل | 672.323 ms | **577.705 ms** | **14.1% أسرع** |
| `repair_blocks` على 15 كتلة | 759.440 ms | **674.982 ms** | **11.1% أسرع** |
| زمن `insert_particle_spaces` | 37.186 ms | **7.777 ms** | **79.1% أسرع** |
| CER في benchmark النصي | 13.790% | **13.351%** | تحسن |
| WER في benchmark النصي | 81.882% | **81.542%** | تحسن |
| WER في PDF حقيقي | 3.105% | **1.634%** | تحسن 1.471 نقطة مئوية |
| CER/WER في PDF مولّد | 13.48% / 45.45% | **0% / 0%** | استرداد كامل |
| Branch coverage | 77% | **90%** | +13 نقطة مئوية |

اختبارات PDF المولّد حققت CER وWER صفراً في المسار الهندسي، بينما بقي مسار MuPDF ثنائي الاتجاه عند CER قدره 28.43% وWER قدره 45.45%، وهو تحقق مستقل من أثر الترتيب الهندسي.

#### حدود متعمدة

لم تُدمج مطابقة شكل الجليف `shape_match` لعدم وجود corpus PDF مجهول CMap وبيانات outlines حقيقية تثبت فائدتها. كما لم يُعاد تصميم التخطيط، لأن scaling المقاس بقي قريباً من الخطي ولم يثبت أنه عنق الزجاجة الأول. هذه الخيارات متعمدة لمنع إضافة تعقيد غير مثبت.

#### الاختبارات

اجتازت مجموعة الاختبارات الكاملة، بما فيها اختبارات PDF الحقيقي، واختبارات CMap، واختبارات الجليفات والفراغات. كما اجتاز التغيير `git diff --check` و`py_compile`.

**حلقة thumb_red (بصمة الإبهام الحمراء) — فحص ذهبي canonical**

مصدر الدليل: الكتاب المنشور من safahat.org + 5 صفحات gold يدوية
(`benchmarks/independent_eval/docs/thumb_red/`). **ليس** نصًا مولَّدًا بالذكاء الاصطناعي.

### حلقة 1 — التباسات حروف
| إصلاح جذري | الأثر |
|------------|--------|
| `امل` + تشكيل/مسافة → `الم` | كان 55 موضعًا يفلت بسبب ُ |
| `هذالا` / `روبني` / `حين` / `إلى`… | قائمة مغلقة من محاذاة gold |

### حلقة 2 — المسافات الهندسية
| إصلاح | التفاصيل |
|--------|----------|
| عتبة **percentile** لكل سطر | بدل MAD المحافظ |
| `collapse_midword_spaces` | طيّ `مو ضع` قبل confusions |
| `LayoutConfig.glyph_space_*` | يُمرَّر عبر `cluster_to_lines` |

### حلقة 3 — مسافات أدق + أدوات
| إصلاح | التفاصيل |
|--------|----------|
| percentile q=0.72، min_factor=0.42 | أقرب لعدد مسافات gold / WER |
| `collapse` 1–3 حروف + بادئات `الع`/`الم` | `عاد ي`، `الع صور` دون كسر `بكم في` |
| `insert_particle_spaces` (محافظ) | `كماأن`/`منالعصور`؛ لا يمسّ `منثورة`/`أولاً` |
| confusions إضافية | `جيلاتني`، `قليًلا`، `هذهامل`… |

| مقياس (5 صفحات gold) | raw | حلقة 1 | حلقة 2 | **حلقة 3** | هدف |
|----------------------|----:|-------:|-------:|-----------:|-----:|
| CER | 0.80 | 0.16 | 0.14 | **0.13** | &lt; 0.08 |
| CERlo | 0.76 | 0.02 | 0.03 | ~0.03 | &lt; 0.02 |
| WER | — | 2.4 | 0.97 | **~0.80** | &lt; 0.5 |
| مسافات ص47 | — | 99 | 264 | **~300** / 344 | ≈ |

سكربت: `python benchmarks/independent_eval/eval_thumb_red.py --refresh`

---

## 1.0.1

**تحسينات من كتب عربية منشورة (لا من نصوص مولَّدة بالذكاء الاصطناعي)**

مصدر الدليل: عيّنة الفحص المستقلة `benchmarks/independent_eval/` —
ثلاثة كتب من [صفحات / safahat.org](https://www.safahat.org/)
(*بصمة الإبهام الحمراء*، *مداخل إلى التفكيك*، *وبالحق نزل*).
هذه PDFs **موجودة مسبقاً**؛ ليست من إنشاء مطوّري arafix وليست stress/AI fixtures.

| التحسين | ماذا ولماذا |
|---------|-------------|
| **مسافات هندسية** | كثير من كتب PDF لا تخزّن U+0020 بين الكلمات؛ الفجوة في `x` فقط. `join_glyphs_preserving_ltr` يدرج مسافات بعتبة تكيّفية (وسيط + k·MAD). |
| **التباسات مغلقة** | `arafix.pdf_confusions`: `امل…`→`الم…`، و`كثري/كبري/غري…`→`كثير/كبير/غير…` بقائمة مغلقة + حدود كلمة (لا تلمس *مغري*). |
| **ثقة الوثيقة** | `DocumentResult.confidence` يتجاهل الصفحات شبه الفارغة (&lt;40 محرفاً) حتى لا يسحب الغلاف الثقة إلى ٠. |

- مفاتيح: `LayoutConfig.insert_glyph_spaces` / `glyph_space_k`، و`PipelineConfig.enable_pdf_confusion_repair`.
- مرحلة جديدة: `Stage.REPAIR_PDF_CONFUSIONS`.
- شفافية: التعليقات والـ CHANGELOG يصرّحان بمصدر Safahat؛ القائمة **لا** تتوسّع تخميناً.

### غير مشمول (عمداً)

- تقطيع كلمات معجميّ مفتوح (spellcheck).
- توسيع المعجم النواة من نصوص الكتب (held-out يبقى للفحص).

---

## 1.0.0

**أول إصدار مستقر (Stable)** — بعد اجتياز فحص الإجهاد الشامل (50 حزمة،
FPR = 0٪، RAR = 100٪) وحلّ جميع عيوب الانحدار FLAW_01…FLAW_08.

### ملخّص المسار من 0.9.0 → 1.0.0

| المحور | ما استُقرّ |
|--------|------------|
| **FLAW_01** | نطاقات الصفحات `(ص. 140-125)` → `(ص. 125-140)` |
| **FLAW_02** | لام-ألف مُبهَم + **معجم نواة** مضمَّن (`use_core_lexicon`) |
| **FLAW_03** | مبالغ/أقواس محاسبية `(-USD 1,250.00)` بعد العكس |
| **FLAW_04** | موجيبيك هجين بنوافذ جزئية + CP1256/ISO-8859-6 |
| **FLAW_07** | كشيدة داخل Presentation Forms قبل الطي |
| **FLAW_08** | نقطة الجملة الملتصقة بالسنة (`.2024` → `2024.`) |

### ميزات 1.0.0 (تجميع الإصدارات 0.9.x)

- **معجم النواة** `arafix.lexicon.core` (~1.7k كلمة، zlib+base85، كسول).
- **أنبوب موحّد** لـ `repair_text` / `repair_blocks` / حصاد الوثيقة مع المعجم.
- **LTR ذكي** (`ReorderConfig`): عملات، نسب، تواريخ، نطاقات صفحات، ترقيم جملة.
- **موجيبيك هجين** دون تخريب ASCII/عربي سليم/`café`.
- **قياس**: `scripts/eval_unified.py`، `scripts/stress_test_report.py`،
  fixtures في `tests/fixtures/flaws/` و `tests/fixtures/stress/`.
- **جودة PDF 0.9**: تشكيل هندسي، طيّ هجائن PDF، layout أعمدة/جداول، MCS/DBR/BFE/SHDR.

### توافق

- النواة ما زالت **بلا تبعيّات** (stdlib) للدرجات 0–2.
- Python ≥ 3.9؛ حالة التطوير: **Production/Stable**.
- `PipelineConfig.use_core_lexicon=True` افتراضياً (يُطفأ بـ `False`).

### نشر

- PyPI عبر GitHub Actions + **Trusted Publisher (OIDC)** — انظر `RELEASING.md`.
- الوسم المتوقع: `v1.0.0` (يجب أن يطابق `__version__`).

---

## 0.9.3

**Stress corpus (50 packs) + release gate**

- `tests/fixtures/stress/ultra_complex_corpus.json` — 50 حزمة / 6 محاور إجهاد.
- `scripts/stress_test_report.py` — FPR / RAR / CER / throughput + قرار
  ``APPROVED FOR V1.0.0`` عند FPR=0 و RAR≥98٪.
- `tests/test_stress_corpus.py` — بوابة CI (مع تخطي حزمة 10k فقط).

**P1/D — موجيبيك هجين + ترميزات موروثة (FLAW_04 أخضر)**

- `detect_mojibake`: مسار كامل UTF-8 (CP1252/latin-1) ثم **نوافذ جزئية**
  للمقاطع الهجينة (`Ø§Ù„Ù…ÙCustomer` → `المCustomer`).
- حذف رؤوس UTF-8 اليتيمة قبل اللاتيني.
- مسار **CP1256 / ISO-8859-6** عند إساءة القراءة كـ Latin-1، بعتبة تمنع
  إيجابيات زائفة على `café` / `résumé`.
- انحدار: **كل FLAW_01…08 خضراء** — لا xfail متبقٍ في suite العيوب.

## 0.9.2

**P1 — اتجاه LTR ذكي (نطاقات صفحات، عملات، ترقيم جملة)**

- استعادة جزر LTR بالدرجة (لا عكس أعمى): يحفظ `USD 1,250.00` ويفك `4202`→`2024`.
- `normalize_page_ranges` — `(ص. 140-125)` → `(ص. 125-140)` و`pp.`.
- `repair_inverted_ltr_parens` — `)-USD …(` → `(-USD …)`.
- `relocate_sentence_punctuation` — `.2024` → `2024.` بعد سياق عربي.
- انحدار: FLAW_01 / 03 / 08 خضراء (كانت xfail).

## 0.9.1

**حوكمة قياس + P0: معجم نواة ولام-ألف وكشيدة PF**

### القياس والاختبارات
- `tests/fixtures/flaws/` — حالات FLAW_01…08 من تقارير `add/` مع `manifest.json`.
- `tests/test_flaws_regression.py` — انحدار لـ B (تمرّ) وxfail لـ C/D.
- `scripts/eval_unified.py` — تقييم موحّد (نص/PDF) بثقة **حقيقية** (min/mean
  لكل صفحة، بلا ادعاء 1.00 لشهادات «سليم»).

### معجم النواة
- `arafix.lexicon.core` — قاموس مضمَّن مضغوط (~1.7k كلمة)، تحميل كسول، stdlib فقط.
- `PipelineConfig.use_core_lexicon` (افتراضي `True`) + `lexicon=` للمستعمل.

### أنبوب لام-ألف
- توحيد `repair_text` / `repair_blocks` / حصاد الوثيقة: المُبهَم يُحسَم عند
  توفّر معجم (نواة أو مستعمل) حتى بلا شاهد قاطع.

### تطبيع
- `strip_tatweel_among_presentation_forms` قبل طيّ PF
  (`NormalizeConfig.strip_tatweel_in_pf_runs`).

## 0.9.0

**جودة الاستخراج على PDF حقيقي** — حماية التشكيل، طيّ الهجائن، جزر LTR،
وطبقة قياس علميّة مع بوابات انحدار.

### إصلاحات جوهرية (استخراج + اتجاه)

- **P0 — ارتباط التشكيل بالقاعدة:** ربط علامات Mn بأقرب حرف أساس هندسياً
  (لا بآخر محرف في التيار)، فلا تخرج كلمات تبدأ بفتحة/ضمّة.
- **P2a — أشكال التشكيل الرسومية:** رباطات الشدة+الحركة (`U+FC5E–FC63`)
  وأشكال التشكيل الفاصلة (`U+FE70–FE7F`) تُعامل كعلامات لا كحروف أساس —
  أصل أعطال مثل `رُوِّج` → `رُِّوج` و`حقٌّ` المبعثرة.
- **ترتيب العلامات:** `order_combining_marks` — الشدة قبل الحركات على
  الحرف الواحد.
- **P1 — طيّ الهجائن:** `fold_pdf_homoglyphs` (افتراضيّ) يطوي `ی/ھ/ک` →
  `ي/ه/ك` من خطوط PDF؛ يُطفأ بـ
  `NormalizeConfig(fold_pdf_homoglyphs=False)`.
- **P1 — جزر LTR:** توسيع حماية المقاطع (تواريخ `13-7`، `M/V Ever`،
  `3.5%`، شرطات en-dash) + إعادة ترتيب جزر LTR بـ `seq` التيار بعد فرز
  `x` في `layout.join_glyphs_preserving_ltr`.

### طبقة علمية (`scientific.py`)

مقاييس لا تخفي العطب خلف CER وحده:

| الرمز | المعنى |
|---|---|
| **MCS** | استمرارية الهيكل الحرفي (مورفولوجيا) |
| **DBR** | مخزون التشكيل + **دقة الالتصاق** بالقاعدة |
| **BFE** | إنتروبيا تدفّق الاتجاه مقابل المرجع |
| **SHDR** | معدّل انجراف الهجائن (ی/ھ…) |

```bash
arafix eval file.pdf --truth truth.txt --scientific
```

```python
from arafix import scientific_audit, extract_pdf
print(scientific_audit(open("t.txt", encoding="utf-8-sig").read(),
                       extract_pdf("f.pdf").text))
```

### اختبارات وانحدار

- corpus حقيقي: `tests/fixtures/real_pdf_narrative/` (نص + PDF متطابقان).
- `tests/test_regression_real_pdf.py` — بوابات CER/محتوى/تشكيل/LTR.
- **P2b — `tests/test_scientific_floors.py`:** MCS/DBR ≥ 0.99، attach ≥ 0.99،
  SHDR = 0، BFE Δref ≤ 0.02 — لا تُخفَّض بلا قرار صريح.
- على corpus السرد: DBR attach **1.0**، CER كامل ~**1.4٪** (بقايا زخرفة
  `_` غير موجودة في طبقة PDF).

### واجهة عامة جديدة

- `fold_pdf_homoglyphs`, `order_combining_marks`
- `scientific_audit`, `morphological_continuity`, `diacritic_base_matrix`,
  `bidi_flow_entropy`, `homoglyph_drift`
- `NormalizeConfig.fold_pdf_homoglyphs` (افتراضي `True`)
- `Glyph.seq` + `join_glyphs_preserving_ltr`

### توافق

- النواة ما زالت بلا تبعيّات؛ PDF اختياريّ عبر `arafix[pdf]`.
- السلوك الافتراضي يتغيّر بطيّ الهجائن (مقصود لاسترجاع عربي قياسي).
  للنصّ الفارسي: `fold_pdf_homoglyphs=False`.

## 0.8.0

**الدرجة البنيوية** — أعمدة RTL، ترويسة/تذييل، جداول من هندسة الجليفات.

**جاهزية النشر:** `py.typed`، وصف إنجليزي على PyPI، README ثنائي اللغة،
`DEPLOY.md` / `CONTRIBUTING.md`، مصنّفات أوضح.

**إصلاح CI (macOS):**

- بعض الخطوط تُخرج `U+066C` (فاصل الآلاف `٬`) بدل الفاصلة العربية
  `U+060C` (`،`). طيٌّ سياقيّ في `hygiene`.
- أشكال التشكيل المعزولة (`U+FE78`…) كانت تُفكَّك إلى **مسافة + علامة**؛
  صارت العلامة وحدها. (طيّ التشكيل PF **مبكراً في hygiene يُفسد** العكس —
  يبقى مؤجّلاً بعد الاتجاه.)
- اختبار التكامل يتسامح بسقوط التشكيل على بعض خطوط macOS، ويرفض
  ``نشُرت`` (عنقودٌ خاطئ).
- ``test_measured_not_asserted``: CER دون 1٪ على الحروف (ignore_diacritics)؛
  CER الكامل دون 5٪ لأن Arial على macOS يُسقط ~5 علامات تشكيل من SAMPLE.

### المشكلة

القراءة الهندسية كانت تضمّ كل جليفات السطر الأفقي معاً. في صحيفة
بعمودين يخرج «يسار+يمين» سطراً واحداً ممسوخاً. والجداول تُسطَّح.
والترويسة تختلط بالجسد.

### الحل: `layout.py` — تقسيم بالميزاب لا بمراكز الأسطر

1. **ميازب أفقية (gutters)** على الجليفات أولاً → أعمدة حقيقية  
2. **ترتيب قراءة RTL** (الأيمن فالأيسر) — قابل لـ `ltr`  
3. **شريط ترويسة/تذييل** (٨٪ أعلى/أسفل) يُعزل قبل الأعمدة  
4. **جداول** من محاذاة خلايا داخل العمود → Markdown + `page.tables`  
5. **`repair_per_block`**: كل سطر/خلية تُشخَّص وحدها ثم يُعاد التجميع  

```python
from arafix import extract_pdf, PipelineConfig, LayoutConfig

doc = extract_pdf("paper.pdf", PipelineConfig(
    layout="auto",           # linear | columns | full
    layout_config=LayoutConfig(reading_order="rtl"),
))
doc.pages[0].n_columns
doc.all_tables
```

```bash
arafix extract paper.pdf --layout full --reading-order rtl -v --tables
python examples/make_multicolumn_pdf.py multi.pdf
```

`layout="auto"` (الافتراضي): صفحة عمود واحد تبقى كما في 0.7 — بلا مفاجآت.
`PageResult.layout` / `.blocks` / `.tables` للبنية الكاملة.

## 0.7.0

طبقةُ الاسترجاع تصير **قابلة للتركيب** — نظافة استخراج، كتلٌ مستقلة،
معجم وثيقة، وجسر MarkItDown. بلا كسرٍ لوعد النواة صفر-التبعيّات.

### بوابة النظافة (`hygiene`)

محرّكات PDF تُخرج `U+00A0` بدل المسافة و`U+00AD` بدل الشرطة — فتفشل
مقارنةُ السلاسل وCER يظنّ الخطأ عربياً وهو ترميز. صارت:

| الأثر | العلاج |
|---|---|
| NBSP ومسافات يونيكود | → مسافة عادية |
| soft hyphen | → `-` |

مرحلة `Stage.HYGIENE` تُبلَّغ في التقرير. تُطفأ بـ
`PipelineConfig(enable_hygiene=False)` إن كنت تقيس المحرّك لا تريد إخفاءه.
**اختبارات تكامل PDF على ويندوز خضراء.**

### `repair_blocks` / `fix_table`

كل كتلة (خلية، سطر، تسمية) تُشخَّص وحدها. خليةٌ معكوسة لا تعكس جارةً
سليمة. يقبل `str` و`TextBlock` و`(id, text)` و`dict`.

```python
from arafix import repair_blocks, fix_table, TextBlock
fix_table([["…", "OK"]])
```

وأمر CLI: `arafix blocks` (stdin: سطر = كتلة).

### معجم الوثيقة الداخليّ

`harvest_document_lexicon` + `PipelineConfig(harvest_document_lexicon=True)`
(افتراضيّ في `extract_pdf` و`repair_blocks`): كلمات الصفحة/الكتلة
الصحيحة تحسم «المجالت» في غيرها — بلا ملف معجم خارجيّ.

### MarkItDown

- entry point: `markitdown.plugin` → `arafix`
- `pip install "arafix[markitdown]"` ثم `MarkItDown(enable_plugins=True)`
- `fix_markitdown(result)` و`fix_any` و`wrap_callable` في
  `arafix.adapters` / `arafix.integrations`
- [INTEGRATING.md](INTEGRATING.md)

الاختبارات: ١٥٥ ← +٣٠ تقريباً.

## 0.6.0

بنشمارك خارجيّ جادّ (٣٠ تكراراً، فتراتُ ثقة، اختبارات Welch، cProfile)
بثمانِ توصيات. **القياسُ على صفحةٍ واقعية قلبَ أولويّاتِه.**

### عنقُ الزجاجة واحدٌ لا ثمانية

على صفحةٍ من ٣١٩٥ محرفاً — لا على سلالم اصطناعية:

| | تصنيفُ التقرير | القياس/صفحة | لأطروحةٍ من ٣٠٠ صفحة |
|---|---|---|---|
| `cer` | حرج | **٢٤٦٢ ms** | **١٢ دقيقة ونصف** ☠️ |
| `diagnose` | أولوية عالية | ٦٫٥ ms | ٢ ثانية |
| `repair_text` | أولوية عالية | ٧٫٣ ms | ٢٫٢ ثانية |
| `decode_glyph_name` | عنق زجاجة | ١٫٢ **µs**/نداء | لا شيء |

والمفارقة موجعة: `arafix eval` — الأداةُ المضافة في 0.4.0 **لإثبات
النزاهة** — كانت غير صالحةٍ للاستعمال على الأطاريح التي بُنيت لأجلها.

### العلاج: مايرز الشعاعية-البِتّية (410×)

**خوارزميّ لا تبعيّة.** استُبدلت المصفوفة الكاملة بخوارزمية مايرز
(1999): تُمثَّل حالةُ عمودٍ كاملٍ بعددٍ صحيح، ويُحسب التالي بعملياتٍ
بِتّية عليه. وهنا تنقلب قلّةُ حيلة بايثون فضيلةً — أعدادُه غير محدودة
الدقّة، فعرضُ «الكلمة» عندنا طولُ النمط كلّه لا ٦٤ بِتّاً.

    cer/صفحة:  2462 ms → 6.1 ms        أطروحة:  745 ث → 1.9 ث

ومعها اقتطاعُ البادئة واللاحقة المشتركتين (O(n)، وأكثرُ النصّ متطابق).
**والمسافةُ مضبوطةٌ لا تقريبية.**

### تصنيفُ المحارف: مجموعاتٌ مبنيّةٌ مسبقاً (5.5×)

`is_arabic` و`is_presentation_form` تُناديان مرّةً لكل محرف. استُبدل
بمسح النطاقات فحصُ عضويةٍ في `frozenset` — ١٫٢٤ ms ← ٠٫٢٢ ms على صفحة.
(قدّر التقرير «١٫٢–٢×»؛ القياس ٥٫٥×.) والمجموعتان مبنيّتان من النطاقات
**نفسها**، وثمنُهما ٦٤ كيلوبايت لـ١٢٣٢ نقطة. ولا مجموعةَ لـ PUA عمداً:
١٣٧ ألف نقطة ثمنٌ لا يشتري شيئاً.

    diagnose/صفحة:  6.5 ms → 2.3 ms      repair_text: 7.3 → 3.3

### الضامن: الذكاءُ يُختبَر بالبلاهة

مايرز والمجموعاتُ كلاهما ذكيّ، والسرعةُ بلا ضبطٍ لا قيمة لها. فبقي في
الشيفرة تنفيذان مرجعيّان بليدان — `levenshtein_reference` و`in_ranges` —
**لا للاستعمال بل ليكونا محكّاً**:

- اختبارٌ عشوائيّ: ٣٠٠٠ زوجٍ ضدّ المرجع + حالاتٌ حديّة.
- تصنيفُ المحارف مقارَنٌ بالمرجع على **كل نقطة كودٍ في يونيكود**
  (١٬١١٤٬١١٢) — لا على عيّنة.

### ما رُدّ — بالقياس لا بالرأي

- **«أضف RapidFuzz» (حرج عندهم)**: قيسَت فكانت أسرع ١١× من مايرز. ورُدّت:
  ١٫٧ ث ← ٠٫١٦ ث لأطروحة، لأحدٍ لا وجود له. **غلطةُ `arafix[ocr]` عينها.**
- **«مسارٌ سريع في `grapheme_clusters`» (عالٍ عندهم، «١٫٣–٢×»)**: القياس
  يقول **أبطأ** في الحالين (٠٫٢٦←٠٫٢٧ مع التشكيل، ٠٫٢٤٧←٠٫٣١٦ بدونه).
- **«مسحٌ واحد في `diagnose`» (عالٍ)**: يوفّر ~١ ثانية على أطروحةٍ كاملة.
  ثمنُه ازدواجُ معرفةِ النطاقات — والمجموعاتُ حقّقت المكسب بلا ازدواج.
- **«Rust/Cython»، «ضبط GC»، «مسار reverse بلا regex»**: نسبُ GC كلها
  ≈١٫٠ (ضوضاء)، والباقي أقلّ من ثانيةٍ للأطروحة.

### وثلاثةُ عيوبٍ في البنشمارك نفسه

١. **يتوّج الخطأ.** «الأسرع: `reverse_visual_line` / `naive[::-1]`» —
   والتقريرُ نفسه يصنّفها `comparable: False` لأنها «تُفسد الأرقام
   ودلالةَ الأقواس». مَن يقيس السرعةَ بلا ضبطٍ يتوّج البليد؛ وهذه
   المكتبةُ قائمةٌ أصلاً على أن `[::-1]` خطأ.
٢. **`memory_winner: rapidfuzz peak=0.0`** — صفرُ بايت مستحيل. `tracemalloc`
   لا يرى ما تخصّصه امتداداتُ C خارج مخصِّص بايثون. أثرُ قياسٍ يُعرَض فوزاً.
٣. **`detect_mojibake` مقابل «stdlib» مُعلَّمٌ `passed: false`** — وانظر
   المخرجات: نحن نُعطي `السلام`، و«القرين» يُعيد الموجيبيك كما هو. **القرينُ
   أخفق لا نحن**، و`ftfy` وافقنا حرفاً بحرف. وسمُ «اختلاف» بلا قولٍ مَن
   المصيب يقلب النتيجة.

الاختبارات: ١٤٢ ← ١٥٥.

## 0.5.0

مراجعةٌ خارجية بثمانِ ملاحظات. فُحصت بالتشغيل: **أربعٌ صحّت، وأربعٌ لا.**

### صحّت — والأولى فاضحة

**١. الثقة كانت رقماً بلا معنى.** كانت `_confidence` تضرب كلَّ شاهدٍ في
«كفاية العيّنة»، فتُخرج **٠٫٥٢ لتشخيصٍ قاطع** (فحصُ نطاقٍ حتميّ على
«ﻣﺮﺣﺒﺎ»)، و**٠٫٤٤ لنصٍّ سليم**. والمكتبة التي تُصدر شهادات ثقة لا
تُصدَّق إن كانت شهادتُها لا تفرّق بين اليقين والظنّ.

القسمة صارت ثلاثيّة، و`Diagnosis.defect_confidence` يفصل ثقةَ كل علّة:

| الشاهد | الثقة | لماذا |
|---|---|---|
| قاطع (نطاقٌ أو اختبارٌ جبريّ) | **١٫٠ دائماً** | فحصُ نطاقٍ على ٥ محارف قاطعٌ كفحصه على ٥٠٠٠ |
| ظنّيّ (الاتجاه) | بدرجة شاهده | |
| «سليم» — شهادةُ نفي | ٠٫٣ ← ٠٫٩ بالحجم | **وحدَها** يحكمها الحجم. وسقفُها ٠٫٩ عمداً: غيابُ العلّة ليس برهانَ سلامة |

**٢. `diagnose` كان أعمى بشاهدين من ثلاثة** — والملاحظة صحيحةٌ في
نتيجتها خاطئةٌ في تعليلها. لم يكن يُضعف شاهد الوصل (فهو يستقبل النصّ
الخام فيه الأشكال)، بل يُعمي الشاهدين **الحرفيين**: التاء المربوطة
مخبوءةٌ خلف `U+FE93`، وهي أقواها وزناً (٠٫٥٠). قِسناه: `0.79` بشاهدٍ
واحد مقابل `0.927` بالثلاثة. فصار `diagnose` يطبّع المفردات داخلياً
للشواهد الحرفية ويُبقي الخام شاهداً على الوصل — ولا يزال لا يكتب شيئاً.

وكشف اختبارُنا الجديد ثغرةً في الإصلاح نفسه: مسارا الموجيبيك والصفحة
الفارغة كانا يعودان مبكراً فيتخطّيان حاسب الثقة. **لا مخرجَ يتخطّاه الآن.**

**٣. حزمة `arafix[ocr]` كانت وعداً بلا سند** — تجرّ `pytesseract` ولا
كودَ يستعمله. حُذفت. لا نبيع تبعيّةً مقابل نيّة. والدرجة ٤ مُعلَّمة `✗
غير منفَّذة` في سلّم الـ README نفسه لا في الحواشي.

**٤. تغطيةُ «لا تمسّ السليم» والكشيدة ضعيفة.** كان الاختبار جملةً واحدة
نظيفة — والمحايداتُ والتشكيلُ والكشيدة هي مواطنُ الأذى. صار ٩ حالات
تشمل الأقواس والتشكيل والنسب والشرطات، و٤ اختباراتٍ للكشيدة.

### لم تصحّ

- **«الدرجة ٣ لم تُختبر كفايةً»**: مغطّاة بـ `decode_glyph_name` وبناء
  الخريطة واستخراج الخطوط من PDF حقيقيّ. والقصورُ الحقيقيّ فيها مُعلَنٌ
  سلفاً (خطوط CID) لا مخفيّ.
- **«التسمية تُضعف الثقة»**: ليست عطباً بل حالةٌ نُشهرها في أول الـ README.
- **«الدوال المنفصلة خطرة»**: صحيحٌ أن `fold_presentation_forms` ثم
  `fix_order` يُعطب. لكن إخفاءها يُكلّف التركيب، والخطر موثّقٌ في
  الـ docstring و**مُخلَّدٌ اختباراً** (`test_the_bug_reproduced_if_order_is_wrong`).
  و`repair_text` هو المدخل المُوثَّق.
- **«الاختبارات ملفّان فقط»**: العددُ ليس مقياساً. والقصورُ الحقيقيّ
  (الأعمدة المتعدّدة والجداول) مُعلَنٌ في «حدودٌ مُعلَنة»، و`arafix eval
  --compare` موجودٌ ليقيسه المستعمل على ملفاته.

الاختبارات: ١١٩ ← ١٤٢.

## 0.4.0

بحثَ مستعملٌ عن الاسم على GitHub فوجد أربعة مستودعات تحمله. قراءتُها
أفادت أمرين: تضاربُ اسمٍ حقيقيّ، وثغرةٌ في نزاهتنا.

### القياس (`arafix eval`)

الأربعةُ كلها تحمل `evaluate` ونحن لم نكن نحمله. وكان قولنا «٠ إخفاق من
١٢» مقيساً على ملفاتٍ **ولّدناها بأنفسنا** — أي يقارب الاختبار الدائريّ
الذي عبناه على أنفسنا في 0.3.0. ثغرةٌ في النزاهة قبل الميزات.

- وحدة `arafix.evaluate`: `cer`، `wer`، `levenshtein` (بصفّين لا بمصفوفة:
  الذاكرة O(min) — صفحةُ أطروحة ٣٠٠٠ محرف والمصفوفة تسع ملايين خانة)،
  `evaluate_text`، `evaluate_pdf`، `compare_extractors`.
- `arafix eval file.pdf --truth truth.txt --compare` يقيس **كل المسارات
  على ملفك أنت** ويرتّبها. أوّلُ رقمٍ لنا::

      pymupdf      CER  0.00%   WER  0.00%
      mupdf-bidi   CER 30.51%   WER 48.48%

- بلا تبعيّات (لا `jiwer`)، ويعدّ الترقيم والتشكيل افتراضياً: تجاهلُهما
  يرفع الدرجة كذباً ويمحو أصدق ما يقيسه الاختبار.
- يسرد أسوأ السطور: المعدّل يقول «٣٪ خطأ» ولا يقول أين.

### التسمية

`arafix` شاغرٌ على PyPI، **مأخوذٌ على GitHub بأربعة مستودعات**، وأحدها
(`AraFix-V3.0`) يوفّر حزمةً عليا اسمها `arafix` بالحرف. التضارب حقيقيّ
لا نظريّ: من ثبّت الاثنين، آخرُهما يفوز بـ `import arafix` صامتاً.
أُضيف تنبيهٌ في الـ README وقسمُ «التسمية والجيران». بدائل شاغرة:
`warraq` (الورّاق)، `qirtas`، `raqim`، `tanqih`.

### الجيران

- ثلاثةٌ من الأربعة مشروعٌ واحد (V1→V3): غلافُ نموذج HuggingFace يصحّح
  التشكيل والأخطاء الصوتية. لا تقاطع.
- `CAMeL-Lab/arafix_ocr` (NYU أبوظبي): تصحيحُ OCR بـ n-gram. **متكامل لا
  منافس** — يبدأ من حيث تنتهي درجتُنا الرابعة.
- ما زال ينقصنا: نموذجٌ لغويّ يحسم «المجالت» بدل المعجم الساذج، وإخراجُ
  PDF قابل للبحث.

الاختبارات: ١٠٦ ← ١١٩.

## 0.3.0

سأل مستعملٌ سؤالاً بسيطاً: «هل تبقى الأقواس حول كلمتها أم ينقلب أحدها؟»
فكشف السؤالُ **ثلاثة أعطاب**، لا واحداً.

### قياسٌ أوّلاً

على ١٢ سطراً فيها ترقيم، مرّرناها في ملف PDF حقيقيّ:

| المسار | الإخفاق |
|---|---|
| `get_text()` — بِدي MuPDF | ٩ من ١٢ |
| القراءة الهندسية + arafix | ٠ من ١٢ |

الحروف تخرج سليمةً والمحايدات مبعثرة::

    (مقدمة الدراسة)   →  ()مقدمة الدراسة
    الفقرة [أ-ج] هنا  →  ج[ هنا-الفقرة ]أ
    توقف!             →  !توقف

و«؟» و«؛» تنجوان وحدهما — لأنهما عربيّتان (صنف AL) لا محايدتين.

### العطب ١: بِدي المحرّك يبعثر المحايدات

- محرّك PyMuPDF صار يقرأ **تيار الرسم الخام** ومواضع الجليفات، ويترك
  الدرجة ٢ تعكس بمنطقنا (`bidi="geometry"`، وهو الافتراضيّ الآن).
- ونقرأ بـ `get_texttrace` لا `rawdict`: الأخير يعيد ترتيب محارفه ببِدي
  MuPDF قبل تسليمها، فلا تيارَ فيه أصلاً (أوّلُ محرفٍ يأتي من أقصى اليمين).
- ولكلٍّ من ربط العنقود وترتيبه شاهدٌ مختلف: **الربط من التيار** (الهندسة
  تكذب: العلامة عرضُها صفر فتُرسَم عند القلم بعد أن تجاوز حرفَها، فـ x
  عندها هو x للحرف التالي)، **والترتيب من الهندسة**.
- `bidi="mupdf"` باقٍ للمقارنة.

### العطب ٢: العكس كان على المحارف لا العناقيد

    أولاً  →  أوًلا        ثانياً.  →  ثانيا.ً

علامة التشكيل عرضُها صفر وتشترك في موضع حرفها. أُضيف `grapheme_clusters`
وصار `reverse_visual_line` يعكس العناقيد ويحفظ داخلها (`cluster_aware`).

### العطب ٣: أشكال التشكيل الفاصلة كانت تُطبَّع مبكراً

    نُشرت  →  نشُرت        المتغيّر  →  المتغيرّ

`U+FE79` («ضمّةٌ بشكلٍ فاصل») فئتها `Lo` لا `Mn` — محرفٌ قائم بذاته.
وتطبيعُها يحيلها علامةً لاصقة، فتنقلب وحدةُ العكس. جريمةُ الرباط نفسها
بثوبٍ آخر.

- استُبدل بمعيار «طول التفكيك» معيارُ **تغيُّر بنية العنقود**
  (`_changes_cluster_structure`). ومعيار الطول كان يُعمي عن هذا الصنف:
  تفكيك `U+FE79` هو [كشيدة + ضمّة]، ونحن نطرح الكشيدة فيعود الطول واحداً.
- `DEFERRED_PF_TO_BASE` (٤٨٩) = رباطات (٤٨٣) + تشكيلٌ فاصل (٦).
- `expand_ligatures` → `expand_deferred_forms` (الاسم القديم باقٍ للتوافق).

### وأعطابٌ صغرى كشفها السؤال نفسه

- `GDP_2024` كان يخرج `2024_GDP`: الشرطة السفلية لم تكن في المقطع اللاتينيّ.
- «توقف!» لم تكن تُعكس: حارسُ كفاية العيّنة كان يردّ **البرهان** كما يردّ
  الإحصاء. وهويّة الوصل برهانٌ، والبرهان لا يحتاج عيّنة — فأُعفي وحده.

### الأقواس: الجواب

تبقى حول كلمتها، **بشرط المرآة**. القوس يُخزَّن بجليفه المرسوم، وجليفُ
أقصى اليسار في سطرٍ عربيّ هو «(» وإن كان المحرف المنطقيّ هناك «)». فالعكس
وحده يعطي «)مقدمة(». والمرآة تشمل `() [] {} <> «» “”` — ولا تشمل `؟ ؛ ! .
, - _` فليست مِرآتية، ومرآتُها تخريب.

الاختبارات: ٦٩ ← ١٠٦.

## 0.2.0

### إصلاح عطبٍ جوهريّ: انقلاب رباط لام-ألف

بلّغ مستعملٌ أن كلماتٍ تخرج هكذا: `االنترنيت`، `المجالت`، `األطاريح`،
`اإلجراء`. العطب كان **في المكتبة**، ونتيجةً مباشرة لقرارها المعماري
الخامس («التطبيع قبل الاتجاه»).

**السبب.** «ﻻ» جليفٌ واحد في PDF — والرباط في العربية إلزاميّ لا
اختياريّ. كانت الدرجة ١ تفكّه إلى حرفين، ثم تعكس الدرجةُ ٢ السطرَ
فتعكس الحرفين معه: «لا» ← «ال».

**الإصلاح.**

- شُطِّر التطبيع حول الاتجاه: `١أ مفردات ← ٢ اتجاه ← ١ب رباطات`.
  الرباط يبقى ذرّةً حتى يستقرّ الترتيب. (`fold_simple_forms`،
  `expand_ligatures`)
- قُسِم `PF_TO_BASE` إلى `SIMPLE_PF_TO_BASE` (٢٤٨) و`LIGATURE_PF_TO_BASE`
  (٤٨٣) بمعيارٍ **مشتقّ** لا مكتوبٍ بيد: طولُ التفكيك. فالعطب لم يكن في
  لام-ألف وحدها بل في ٤٨٣ رباطاً.

### عطبٌ ثانٍ انكشف تحت الأول: عمى الكاشف بعد التطبيع

التطبيع يمحو صيغ الوصل، وهي شاهدُ كاشف الاتجاه. فكان يفتح عيناً
(التاء المربوطة) ويفقأ أخرى.

- صار `detect_visual_order` يقبل `shaped_source=` فيقرأ الطبقتين معاً.
- استُبدل بفحص طرفَي الكلمة **هويّةُ الوصل**:
  `joins_forward(a) == joins_backward(b)` لكل متجاورين — لا تتخلّف في
  نصٍّ منطقيّ، فخرقُها برهانُ انعكاس. شاهدٌ لا تماثليّ: يدحض ولا يُزكّي.
  (الفحص القديم كان يُفلت كلماتٍ كـ«الإجراء».)

### جديد

- `arafix.lamalef` — ترقيع رجعيّ لنصٍّ أعطبته أداةٌ أخرى:
  قاطعٌ حتميّ (ألفان متجاورتان) + مدخل `lexicon=` للمُبهَم.
- `Defect.LAM_ALEF_TRANSPOSED`، `Stage.EXPAND_LIGATURES`،
  `Stage.REPAIR_LAM_ALEF`، `PipelineConfig.lexicon`.

### سبب فوات العطب

مولّد العيّنات لم يكن يُنتج رباطات — أي أن ٤٥ اختباراً خضراء كانت
تختبر المكتبة على عالمٍ لا «لا» فيه. أُضيف `ligate()` إلى المولّد،
وأُضيفت الكلمات الأربع المُبلَّغ عنها اختباراتٍ دائمة، وفخاخٌ يجب ألّا
تُمسّ («أفعالهم لا تطابق أقوالهم»، «قال»، «جمال»، «لآلئ»).

الاختبارات: ٤٠ ← ٦٩. الـ doctests: ٦ ← ١٠.

## 0.1.0

الإصدار الأول. الدرجات ٠–٣.
